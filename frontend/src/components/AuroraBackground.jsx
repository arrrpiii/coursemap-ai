// Previously rendered floating neon orbs + a page-level aurora that
// followed the cursor. Both were layered gradient effects and have been
// disabled at the user's request. Kept as a no-op so Layout.jsx's import
// stays valid.
export default function AuroraBackground() {
  return null;
}
